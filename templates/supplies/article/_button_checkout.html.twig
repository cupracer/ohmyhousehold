<a href="{{ targetUrl }}" class="btn btn-secondary btn-icon-split btn-sm {{ extraClasses|default('') }}">
    <span class="icon text mr-1">
        <i class="far fa-minus-circle"></i>
    </span>
    <span class="text">{{ titleKey|default('app.supplies.article.form.checkout.title')|trans|u.title }}</span>
</a>